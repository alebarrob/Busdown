package barrera.alejandro.busdown.model.enum

enum class Error(val message: String) {
    INCORRECT_EMAIL_FORMAT("Incorrect format. Remember to separate each email with commas, e.g. email1@gmail.com, email2@gmail.com")
}